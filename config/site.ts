export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Supplier Invoice Data AI Data Extractor",
  description: "Extract and manage supplier invoice data with AI.",
  navItems: [
    {
      label: "Home",
      href: "/",
    },
  ],
};
